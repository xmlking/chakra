{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "imports": {
    "#components/*": "./src/components/*.tsx",
    "#hooks/*": "./src/hooks/*.ts",
    "#lib/*": "./src/lib/*.ts"
  },
  "scripts": {
    "build": "tsc && vp build",
    "clean": "git clean -xdf test-results .cache dist coverage node_modules",
    "dev": "vp dev",
    "preview": "vp preview"
  },
  "dependencies": {
    "@workspace/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  }
}
